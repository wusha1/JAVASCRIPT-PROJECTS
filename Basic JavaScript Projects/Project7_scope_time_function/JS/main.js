function Check_Number() {
    var Number = 10;

    // Simple if statement
    if (Number > 5) {
        document.getElementById("If_Result").innerHTML =
            "The number is greater than 5.";
    }
}
