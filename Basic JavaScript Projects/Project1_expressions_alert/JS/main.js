// This alert displays a message when the page loads
window.alert("Welcome to my Basic JavaScript Project!");

// These variable store string values
var Sent1 = "This is the beginning of the string";
var Sent2 = "and this is the end of the string";

// This line concatenates the two strings and writes them to the page
document.write(Sent1 + Sent2);

// This is an example of a simple expression
var result = 5 + 10;

// This writes the result of the exprssion to the page
document.write("<br>Expression result:" + result);